{
  "name": "luminarysmartspace",
  "version": "0.3.0",
  "description": "LuminarySmartSpace - Smart project tracking for Claude Code with workspace support",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @luminarysmartspace/mcp-server && npm run build -w @luminarysmartspace/cli",
    "build:server": "npm run build -w @luminarysmartspace/mcp-server",
    "build:cli": "npm run build -w @luminarysmartspace/cli",
    "dev": "npm run dev -w @luminarysmartspace/mcp-server",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules",
    "setup": "npm install && npm run build && cd packages/cli && npm link",
    "init-project": "node scripts/init-project.js",
    "postinstall": "echo '\n✨ Run: npm run setup\n   This will build packages and link the CLI globally\n'"
  },
  "keywords": [
    "claude-code",
    "mcp",
    "project-management",
    "luminary",
    "ai-tools"
  ],
  "author": "CreatorLoCC (https://github.com/CreatorLoCC)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/CreatorLoCC/Luminary.git"
  },
  "homepage": "https://github.com/CreatorLoCC/Luminary#readme",
  "bugs": {
    "url": "https://github.com/CreatorLoCC/Luminary/issues"
  }
}
